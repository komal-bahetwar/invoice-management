// Azure Cache for Redis — caching layer (future)
// SKU: Basic C0 (staging, 250MB), Standard C1 (prod, 1GB with data persistence).
// Pattern: cache-aside for dashboard queries (TTL-based invalidation on invoice mutations).
